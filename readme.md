# Command basic to run
 - docker-compose up -d
 - docker exec kafka kafka-topics --version

## make enviroment & activate
- python3 -m venv env_kafka_python
- source env_kafka_python/bin/activate

## install
- pip install -r requirements.txt
